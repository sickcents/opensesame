# Ralph loop progress log
# Format: [ID] [Title] - DONE | FAILED (reason) | BLOCKED (reason)

[1] Project scaffold — Next.js + Neon Postgres/PostGIS, base schema - DONE
[2] Floor Plan extraction — PDF upload to text-stripped SVG - DONE
[3] Scale Calibration — two-point real-world distance per Floor - DONE
[4] Geo-Anchoring — pin + rotation per Facility - DONE
[5] Equipment Type catalog + Equipment placement - DONE
[6] Room and Area polygon editor with PostGIS storage - DONE
[7] Safety Equipment point markers - DONE (also fixed issue #5's Equipment to use PostGIS Point geometry per ADR-0006)
[8] 3D view with Equipment extrusion - DONE
[9] World map facility overview - DONE
[10] Issue reporting and resolution workflow - DONE
[11] Gemini structured spatial query tools - DONE
[12] Floor picker as a map-overlay control - DONE (also added the repo's first test setup: Vitest + RTL, per issue #12's testing decisions)
[13] OSM building reference overlay, occupied-portion segmentation, and reanchor auto-zoom - DONE (also fixed a latent drizzle-kit db:push data-loss trap: missing extensionsFilters was proposing to drop PostGIS's spatial_ref_sys table)
[14] Facility query pathfinding: accessibility/PPE/obstacle-aware walking routes - DONE (grid-rasterized A* in lib/pathfinding.ts; getWalkingPath tool skips a live-integration test since no Floor-level geometry is seeded yet — pure-function tests cover the algorithm)
[15] Floor Plan Editor Mode: select, rotate/snap, layer panel, ruler, color - DONE (built in 4 phases: schema+pure logic, edit-mode/pan-zoom/selection, layer-panel/color-picker/ruler, snapping/close-snap/undo; verified interactively against live seed data, not just automated tests, given the size and interaction-heavy surface of this one)
[16] Redesign the Issues panel into an interactive workspace - DONE (card list with Department filter chips + counts, Newest/Department sort-group toggle, relative-time age display with a 3-day aging accent, click-to-highlight linking a card to its subject on both 2D and 3D views via a new read-only highlightRef prop kept separate from Floor Plan Editor Mode's edit-selection state; verified interactively against live seed data with open Issues across two Departments)
[17] Overpass API returns 406 when fetching OSM way outline - DONE (added Accept: application/json and a descriptive User-Agent header to the fetch call in lib/osm.ts, since Node's fetch sends neither by default and Overpass rejects requests missing them; error path now also surfaces the response body text, truncated to 200 chars, for easier debugging of future non-200 responses)
[18] Add toggleable chat drawer to Floor view with waypoint rendering in 3D - DONE (moved AskPanel to app/components/ask-panel.tsx so both Facility and Floor pages can use it; FloorWorkspace now owns a closed-by-default drawer plus the active route, seeded from the ?route= query param but overridable in place — clicking "View route" in the drawer for the Floor currently open updates local state instead of navigating, so drawer/chat history survives; FloorViewSwitcher threads routeWaypoints/routePpeAreas into Floor3DView too, which now draws the same dashed-cyan path via drei's Line plus a PPE-required banner, mirroring the existing 2D rendering; verified interactively — drawer toggle, draft-input persistence across 2D/3D switches, and 3D route/PPE rendering via a synthetic ?route= param all confirmed live, though the live LLM round-trip itself couldn't be exercised since GOOGLE_GENERATIVE_AI_API_KEY isn't set in this environment)
[19] Select OSM way reference by clicking the map, alongside link/ID paste - DONE (verified parseOsmWayId already handled the www./query-string/fragment/surrounding-text variants correctly — locked that in with new regression tests, no fix needed there; added fetchNearestBuildingWay to lib/osm.ts, an Overpass around: query that picks the building way whose centroid is closest to the clicked point via haversine distance, refactoring fetchOsmWayOutline and it onto a shared postOverpassQuery helper; GeoAnchorMap gained a "Pick building on map" mode, mutually exclusive with occupied-portion drawing, that previews the found way as a dashed amber polygon before confirming via the same setOsmReference action the paste flow uses — one path from way ID to attached outline either way; verified interactively against the live Overpass API: the "no building found" empty-result message rendered correctly for an unmapped desert coordinate, and a genuine transient 504 from Overpass was caught and displayed as a readable error rather than crashing the page — also incidentally reconfirming issue #17's fix, since the request now reaches Overpass instead of being rejected at the edge with 406)
[20] Group the 2D editor's top toolbar into a shallow contextual menu - DONE (added a reusable ToolbarGroupMenu to floor-plan-canvas.tsx — a trigger button that reveals its items in a click-outside-to-close panel, highlighting itself when any item inside is active; Equipment-type buttons, Draw room/area, and Safety-kind buttons each moved into their own group, 2 clicks from toolbar to armed; Select, Calibrate/Recalibrate, Undo, Redo, and Done stayed top-level per the spec. Found and fixed a stacking-order bug during interactive verification: the menu's click-outside backdrop (z-30) sat above the trigger button, which had no z-index, so re-clicking an open trigger to toggle it closed silently failed — gave the trigger `relative z-40` to fix it. Verified interactively: opening Equipment, arming Server Rack, placing it on the canvas, and undoing all worked exactly as before regrouping)
[21] Add clockwise/counter-clockwise rotate-90° icon buttons (Phosphor Icons) - DONE (added @phosphor-icons/react, scoped to this control only; added a rotate90(current, "cw"|"ccw") sibling helper to lib/rotation.ts rather than changing normalizeRotation90's signature, since that function is also used to validate persisted values; FloatingToolbar's single "Rotate 90°" button became two ArrowClockwiseIcon/ArrowCounterClockwiseIcon buttons, and floor-plan-canvas.tsx's rotateSelectedEquipment + the R keyboard shortcut now take a direction, with Shift+R added for CCW — disambiguated via e.shiftKey rather than e.key's case, since Shift also capitalizes e.key on its own. Verified interactively against live seed data (Server Rack, 0.6m x 1.0m so rotation is visually obvious): both icon buttons and both R/Shift+R shortcuts rotate the correct direction and snap to {0,90,180,270}, undo/redo history records each step, and CW-then-CCW returns the item to its exact starting orientation)
